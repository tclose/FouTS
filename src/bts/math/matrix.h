#include "math/matrix"

namespace FTS {
    
    namespace Math {
        
        // For convenience typdef MR::Math::Matrix<double> to Math::Matrix
        typedef MR::Math::Matrix<double> Matrix;
    
    }
}
